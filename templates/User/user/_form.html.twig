
{{ form_start(form) }}
<div class="row">
    <div class="col-md-6">
        <div class="form-group">
            <button type="button" id="roles-strategy" class="btn btn-dark"> ROLES </button>
            <button type="button" id="groupe-strategy" class="btn btn-info"> GROUPE </button>
            <button type="button" id="user-strategy" class="btn btn-danger"> USER </button>
            <span id="show-roles-strategy" >
            <div class="form-group">
                <label>Roles</label>
                {{ form_widget(form.role, {'attr': {'class': 'form-control select2'}}) }}
            </div>
            <div class="form-group" >
                <label>Grant Permission</label>
                {{ form_widget(form.grantPermission, {'attr': {'class': 'form-control select2'}}) }}
            </div>
            <div class="form-group">
                <label>Revoke Permission d</label>
                {{ form_widget(form.revokePermission, {'attr': {'class': 'form-control select2'}}) }}
            </div>
            </span>
            <span id="show-groupe-strategy" style="display: none">
            <div class="form-group">
                <label>Groupes</label>
                {{ form_widget(form.groupes, {'attr': {'class': 'form-control select2'}}) }}
            </div>
            </span>
            <span id="show-user-strategy" style="display: none">
                <div class="form-group">
                <label>Users clone</label>
                {{ form_widget(form.userClone, {'attr': {'class': 'form-control select2'}}) }}
            </div>
            </span>
                {{ form_rest(form) }}
        </div>
        <div class="col-md-2" >
            <button id="user_random_password" type="button" class="btn btn-info"><i class="fa fa-key"></i></button>
        </div>
        <br><br>
    </div>

    <div class="col-md-12">
        <a  class="btn btn-info float-left" type="button">Back to the list </a>
        <button class="btn btn-primary float-right" type="submit">Save</button>
    </div>
{{ form_end(form) }}
    {{ encore_entry_script_tags('user') }}

