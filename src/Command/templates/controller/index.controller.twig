<?php
declare(strict_types=1);
namespace App\Http\Controller\{{ namespace }};
use Symfony\Bundle\FrameworkBundle\Controller\AbstractController;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\Routing\Annotation\Route;
use App\Core\Datatable\Factory\DataTable;
use Symfony\Component\HttpFoundation\Request;
use App\Core\Datatable\Option\{{namespace}}TypeOption;
use App\Domain\{{domain}}\Entity\{{namespace}};
/**
* @Route("{{route}}", name="{{route_name}}",options = { "expose" = true })
*/
class {{ class_name }} extends AbstractController
{
    private DataTable $dataTable;
    public function __construct(DataTable $dataTable)
    {
        $this->dataTable = $dataTable;

    }
    public function __invoke(Request $request):Response
    {
        if ($request->isXmlHttpRequest()){
            return  $this->json($this->dataTable->setEntity({{name}}::class)->setTypeButtons({{name}}TypeOption::TYPE)->execute());
        }
        return $this->render("{{view}}");
    }
}